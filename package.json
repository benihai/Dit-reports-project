{
  "name": "dit-report",
  "version": "1.0.0",
  "private": true,
  "description": "Server-side dependencies for the Netlify PDF-render function. The app itself is build-free static files; these deps are only used by netlify/functions/render-pdf.js.",
  "dependencies": {
    "@sparticuz/chromium": "131.0.1",
    "puppeteer-core": "23.11.1"
  }
}
